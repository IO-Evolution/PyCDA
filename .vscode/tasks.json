{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Generate CDA JSON",
            "type": "shell",
            "command": "source .env/bin/activate && python gen.py > data.json"
        },
        {
            "label": "Generate CDA XML",
            "type": "shell",
            "command": "source .env/bin/activate && python gen.py"
        }
    ]
}